name: Keep Render Server Alive

on:
  schedule:
    # This runs every 10 minutes
    - cron: '*/10 * * * *'
  workflow_dispatch: # Allows you to click a button to run it manually

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend to Wake It Up
        # 👇 REPLACE THIS URL WITH YOUR REAL RENDER BACKEND URL 👇
        run: curl -I https://querystream-api.onrender.com/health